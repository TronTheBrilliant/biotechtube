import ProfileEditsListClient from './ProfileEditsListClient'

export const metadata = { title: 'Profile Edits | Admin | BiotechTube' }

export default function ProfileEditsPage() {
  return <ProfileEditsListClient />
}
