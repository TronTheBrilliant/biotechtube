import CommandCenterClient from "./CommandCenterClient";

export const metadata = {
  title: "Command Center | BiotechTube Admin",
};

export default function CommandCenterPage() {
  return <CommandCenterClient />;
}
